import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'LinkPay BD - Payment Links for Bangladeshi Freelancers',
  description: 'Create and send professional payment links in under 30 seconds. Accept international card payments with automatic settlement to bKash or bank.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
